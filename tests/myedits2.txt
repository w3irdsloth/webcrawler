So all will be good. In this case it's probably simpler to just forcepush. Hostname github. At least, that's the normal process. Making statements based on opinion back them up with references or personal experience. Git itself has no special branch names. The problems are that it's loaded with jargon webster wikipedia, and abbreviates quotsourcequot to quotsrcquot. Git is all about commits. It's just a boilerplate commit, after all. For instance, a tag name works fine. Host github. The git push part is the command that means send commits and ask them to set a name. There are a bunch of other string literals reading master in the source and documentation as well they're being converted to use the configuration settings but this will all take time. There are some technical flaws in this kind of claim. This is where things are going wrong. Ive one more request. Independent researcher being solicited for an academic collaboration. Note ided. Please be sure to answer the question. Updates were rejected because a pushed branch tip is behind its remote hint counterpart. The resulting repository will also have two root commits. Pattern matching vs. To obtain their commit, simply run git fetch origin. None of these are serious problems, but they might prove slightly annoying. If not, don't worry too much about them right now. Creating that initial commit actually creates the branch name main. Added test. 